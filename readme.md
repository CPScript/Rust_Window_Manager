Make sure you can SSH into your machine with X11 forwarding enabled. You can do this using: `ssh -X username@host`

You will need to link against X11 libraries, so ensure you have the necessary development packages. On Ubuntu, you can install them using: `sudo apt install libx11-dev
`

